As an app developer, I want to include the code of a dataset type in my app artifact and create a dataset of that type when deploying the app. As a hydrator user I want all dataset instances created by apps are available as sinks and sources for pipelines.